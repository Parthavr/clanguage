#include<stdio.h>
int main()
{
    int a,b,c;
    int num,reverse;
    printf("enter 3 digit number:");
    scanf("%d", &num");
    num / 100;
    (num/10)%10;
    num %10;
    reverse =  c* 100 + b * 10 + a;
 printf("the reverse number is = %d", reverse);
    return 0;

}